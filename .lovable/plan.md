# Correct Bavaria service-area schema and complete the audit

## Changes
- Change the shared Organization and contact-point `areaServed` values to Bavaria only.
- Preserve `addressCountry: "DE"` for both real Munich business addresses.
- Verify the resulting schema in the preview and confirm the current build remains healthy.

## Audit
- Produce a complete audit in chat with every affected route listed separately, including all 9 service pages and all 9 project pages.
- For each entry, show language, exact section or SEO field, full original text, and full updated text; mark unverifiable originals as unavailable.
- Separate shared-component effects from page-specific changes and clearly distinguish preview verification from production publication.
